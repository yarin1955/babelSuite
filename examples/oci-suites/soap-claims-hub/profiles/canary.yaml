name: Canary Partner
description: Partner-like SOAP headers and stricter claim review toggles.
default: false
runtime:
  suite: soap-claims-hub
  repository: localhost:5000/enterprise-integration/soap-claims-hub
  profileFile: canary.yaml
modules:
    - runtime
observability:
  logs: structured
  traces: enabled
  metrics: enabled
