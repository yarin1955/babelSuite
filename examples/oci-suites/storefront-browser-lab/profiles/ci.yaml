name: CI Browser
description: Headless Playwright suite with tighter timeouts and deterministic mocks.
default: false
runtime:
  suite: storefront-browser-lab
  repository: localhost:5000/qa/storefront-browser-lab
  profileFile: ci.yaml
modules:
    - kafka
    - playwright
    - mock-api
observability:
  logs: structured
  traces: enabled
  metrics: enabled
